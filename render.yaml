services:
  - type: web
    name: savanna-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn wefixit.backend.main:app --host 0.0.0.0 --port 10000
    plan: free
